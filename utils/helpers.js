// import { urlFor } from "./client";
// import { getPlaiceholder } from "plaiceholder";

// export const getBase64Array = async (imagesArr) => {
//   const images = await Promise.all(
//     imagesArr.map(async (image) => {
//       const { base64 } = await getPlaiceholder(urlFor(image).url())

//       return {
//         image: image,
//         base64: base64
//       }
//     })
//   )

//   return images
// }

// export const getBase64Image = async (src) => {
//   const image = new Promise(
//     const { base64 } = await getPlaiceholder(urlFor(src).url())

//   )

//   return {
//     image: src,
//     base64: base64
//   }
// }
